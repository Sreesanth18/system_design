Important poins

When you're unsure where a method belongs, ask:

"Is this operation modifying/managing this particular object, or managing a collection/workflow involving many objects?"

{Now once a show is created with a movie you don't add another movie to the same show once a show object is created a movie is already allotted. Show its the responsibility of the showservice to create a show with a movie};